version: '3'

services:
  tornano-back:
    image: tornano-back
    ports:
      - 3001:3001
    restart: always

  tornano-front:
    image: tornano-front
    ports:
      - 3000:3000
    restart: always

networks:
  default:
    name: tornano-network
